# Run with `dart run ffigen --config ffigen.yaml`.
#
# `src/kagipp_ffi.h` is generated by cbindgen from the Rust FFI crate
# (rust/src/ffi). Refresh it via `bash scripts/update_prebuilt_libs.sh`
# before regenerating bindings.
name: PrivacyPassBindings
description: |
  Bindings for `src/kagipp_ffi.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/src/privacypass_ffi_bindings_generated.dart'
headers:
  entry-points:
    - 'src/kagipp_ffi.h'
  include-directives:
    - 'src/kagipp_ffi.h'
ffi-native:
comments:
  style: any
  length: full
